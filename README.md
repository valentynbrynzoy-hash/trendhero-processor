# 🚀 TrendHero Processor — Google Sheets Data Pipeline

 [Google Apps Script ](https://docs.google.com/spreadsheets/d/1gYmKUoZAedBYmePqfgfJgIBxWgtiuFtMkuDYvsgSWYo/edit?usp=sharing) для автоматической обработки масива с почтами\ссылками  другими данными выгруженими из [TrendHero ](https://trendhero.io/): фильтрация по ГЕО, извлечение контактов из bio, сортировка и форматирование результатов. 

Наверное каждый кто хоть раз сталкивался с сервисом TrendHero знает что их база инфлюенсиров не актуальная на 100% в момент выгрузки данных и нуждается  в ручной перепроверки гео и что выбраный на веб сайте гео не означает что в таблице либо в списке каналов все каналы будут с выбраного гео и тем меньше гео, тем хуже выборка каналов. Скрипт TrendHero Processor старается частично решить это, для этого он анализирует колонку Гео с масива данных и работает ТОЛЬКО с нужными выбраными гео.

При правках можно адаптировать под обработку масива данных с других платформ агрегаторов инфлюенсиров или програм парсеров по аналогии apify.com

## 📋 Pipeline

Для работы нужно скачать таблицу с сайта TrendHero <img width="1900" height="390" alt="Screenshot_10" src="https://github.com/user-attachments/assets/73b94ebc-55a3-4bb9-8b81-b55946a479a6" /> отрыть ее и скопировать все колокни все строки в этой таблице.

После этого необходимо добавить скопированый масив с данными с раздел 📥 Входные данные 
<img width="1919" height="265" alt="Screenshot_11" src="https://github.com/user-attachments/assets/3f1e528a-778d-42de-9613-632eae9f833a" />

Перейти в настройки, вставить в раздел разрешенные гео, поле значение нужные гео через запятую, в моем случае это Шри-Ланки, название гео можете взять с самой таблицы трен-хиро в колонке с гео.
<img width="1135" height="747" alt="Screenshot_12" src="https://github.com/user-attachments/assets/7c166b43-15c3-42a4-a89b-4d653ad8cbba" />
и нажать кнопку Трендхиро-Запустить обработку
<img width="1787" height="278" alt="Screenshot_13" src="https://github.com/user-attachments/assets/dcc646c7-0ed2-4b3d-b3b2-7a4b51e78781" />

Дальше дождатся когда скрипт обработает весь масив перейти в раздел 📊 Результат и увидеть все каналы с одним гео, где в начале списка будут каналы с контактами, каналы где нет контактов пусто. Бонусом если почты нет в колонке но есть в био скрипт перенесет контакты найденые в поле contacts и отметит этот канал как канал с контактами

После этого у вас выйдет масив с обработаными даными но не спешите скопировать все полученые таким образом контакты, у вас есть еще большой список каналов нуждающий в ручной перепроверки, пройдите все каналы, добавте в таблицу контакты а затем нажмите на кнопку Контакты вверх который отсортирует все найденые уже вами каналы с контактами вверх и затем уже работайте с ними 
<img width="1276" height="279" alt="Screenshot_14" src="https://github.com/user-attachments/assets/7891f40a-ff4d-4db3-b453-86eae2009b64" />


После окончание работы скопировать  диапазон в рабочую таблицу и уже работать с ними, после повторного использование применить фукнции автоочистки 📥 Входные данные + 📊 Результат и повторить цикл

**Входные данные → одно нажатие → готовый к работе список с контактами**

1. **Фильтр по ГЕО** — оставляет только нужные страны из экспорта
2. **Парсинг контактов** — вытаскивает email и телефоны из поля `biography`
3. **Сортировка** — инфлюенсеры с контактами идут наверх
4. **Форматирование** — username становится кликабельной ссылкой на профиль, строки с контактами подсвечиваются зелёным

### Что извлекается из Bio

| Тип | Примеры |
|-----|---------|
| Email | `name@gmail.com`, `contact@brand.co.th` |
| Телефон с кодом страны | `+66 81 234 5678`, `+380-99-123-45-67` |
| Локальный номер | `0812345678`, `08 1234 5678` |
| Номер через дефис | `081-234-5678` |

Дубли в контактах в рамках одного профиля автоматически убираются.

## 🚀 Установка

### Быстрый старт (рекомендуется)

1. [Открой шаблон таблицы](https://docs.google.com/spreadsheets/d/1430GMpscRhiQzbiMvJAbBN23o_fpBMa_DETFnamb_dY/edit?usp=sharing)
2. **Файл → Создать копию** — сохранить себе на Drive
3. **Расширения → Apps Script** → удали дефолтный код → вставь `Code.gs` → сохрани
4. Закрой вкладку Apps Script, обнови таблицу
5. Появится меню **🚀 TrendHero** → запусти **setupSystem()** (один раз)

### 🔐 Авторизация скрипта

При первом запуске Google попросит дать разрешения.

1. Появится окно **«Требуется авторизация»** → нажми **Продолжить**
2. Выбери Google-аккаунт, к которому привязана таблица
3. Увидишь предупреждение **«Google не проверил это приложение»** — это нормально для личных скриптов
4. Нажми **«Дополнительные настройки»** → **«Перейти на страницу [название проекта] (небезопасно)»**
5. Нажми **Разрешить**

После этого разрешения сохраняются — повторно подтверждать не нужно.

### С нуля

1. Создай новую Google Таблицу
2. **Расширения → Apps Script** → вставь `Code.gs` → сохрани
3. Запусти функцию `setupSystem()` — она создаст все нужные листы
4. Обнови страницу — появится меню **🚀 TrendHero**

## ⚙️ Настройка

Все параметры — на листе **⚙️ Настройки**, менять прямо там:

| Параметр | По умолчанию | Описание |
|----------|-------------|---------|
| Разрешённые ГЕО | `TH` | ISO-коды через запятую: `TH, UA, KZ` |
| Колонка ГЕО | `country` | Имя колонки в данных TrendHero |
| Колонка Bio | `biography` | Имя колонки с описанием |
| Колонка ссылки | `ig_link` | Имя колонки со ссылкой на профиль |
| Извлекать контакты | ✅ | Чекбокс — вкл/выкл парсинг контактов |

## 📁 Структура таблицы

После `setupSystem()` в таблице появятся три листа:

```
⚙️ Настройки     ← меняй параметры здесь
📥 Входные данные ← вставляй экспорт из TrendHero сюда (с row 4)
📊 Результат      ← готовые данные после обработки
```

### Ожидаемые колонки во входных данных

```
username | full_name | ig_link | country | city | languages | biography | follower_count | following_count | media_count | general_er
```

Если в экспорте TrendHero другие названия — поправь соответствующие параметры в настройках.

## 📋 Меню

| Пункт | Действие |
|-------|---------|
| ▶ Запустить обработку | Фильтрация + парсинг + сортировка + запись в «📊 Результат» |
| ↕️ Contacts наверх | Пересортировать результат без повторной обработки |
| 🧹 Очистить результаты | Удалить данные из листа результатов |
| 📋 Очистить входные данные | Удалить данные из входного листа |

## 💡 Нюансы

- **ГЕО-коды** — двухбуквенные ISO 3166-1 alpha-2, заглавными: `TH`, `UA`, `US`, `ID`, `PH`. Список пустой = берутся все страны.
- **Названия колонок** — регистр не важен, `Country` и `country` — одно и то же.
- **Пересортировка** (`↕️ Contacts наверх`) — работает без повторной обработки, сохраняет форматирование и гиперссылки.
- **Очистка входных данных** — заголовки (строки 1–3) не трогаются, только данные.

## 🔧 Технические детали

- Runtime: Google Apps Script (V8 engine)
- Зависимости: нет, только встроенный SpreadsheetApp API
- ES5-совместимый JavaScript (требование Apps Script runtime)
- Тестировалось на экспортах TrendHero 2025–2026
